#include "Console.h"


bool Console::nextCommand(const std::string& command)
{
    // pass (if the command exists, func will return 'true' value)
    std::cout << "Unknown command." << std::endl;

    return false;
}

bool Console::pathIsCorrect(const std::string &path)
{
    // pass
    return true;
}

void Console::createDB()
{
    // pass
}

bool Console::uploadDB(const std::string &path)
{
    // pass
    return false;
}
